<tr>
  <%= content_tag(:td, poem.fragment, scope: 'row') %>
  <td>
    <%= link_to 'View', admin_poem_path(poem) %> |
    <%= link_to 'Edit', edit_admin_poem_path(poem) %> |
    <%= link_to('Delete', admin_poem_path(poem),
                method: :delete,
                data: { confirm: 'Are you sure you want to delete this poem?' }) %>
  </td>
</tr>
