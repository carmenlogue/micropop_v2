<tr>
  <%= content_tag(:td, song.title, scope: 'row') %>
  <td>
    <%= link_to 'View', admin_song_path(song) %> |
    <%= link_to 'Edit', edit_admin_song_path(song) %> |
    <%= link_to('Delete', admin_song_path(song),
                method: :delete,
                data: { confirm: 'Are you sure you want to delete this song?' }) %>
  </td>
</tr>
